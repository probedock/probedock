#!/usr/bin/env sh

ARGS="$@"
APP_LINKS="--link postgres:postgres --link redis:redis --link rox-center-server:rox-center-server --volume /vagrant:/app"

vagrant ssh -c "docker run --rm $APP_LINKS rox-center $ARGS"
