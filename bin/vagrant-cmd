#!/usr/bin/env sh

ARGS="$@"
APP_LINKS="--link postgres:postgres --link redis:redis --link probe-dock-server:probe-dock-server --volume /vagrant:/app"

vagrant ssh -c "docker run --rm $APP_LINKS probe-dock $ARGS"
